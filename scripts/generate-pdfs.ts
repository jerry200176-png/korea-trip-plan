/**
 * @deprecated Legacy PDFKit renderer — failed delivery (see docs/PDF_DELIVERY_FAILURE.md).
 * Use: npm run publication:build
 */
console.error(
  "generate-pdfs.ts is deprecated. PDFKit output is NOT Couple Preview quality.\n" +
    "Run: npm run publication:build"
);
process.exit(1);
